    <%= image_tag @post.image.url(:one_to_one) %>
    <%= image_tag @post.image.url(:two_to_three) %>
    <%= image_tag @post.image.url(:three_to_four) %>
    <%= image_tag @post.image.url(:four_to_five) %>